services:
  drone-sim:
    # NVIDIA dedicated GPU via CDI
    deploy:
      resources:
        reservations:
          devices:
            - driver: cdi
              device_ids:
                - nvidia.com/gpu=all
              capabilities: [gpu]